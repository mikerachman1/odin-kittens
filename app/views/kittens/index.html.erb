<h1>All Kittens:</h1>

<ul>
  <% @kittens.each do |kitten| %>
    <li> <%= link_to kitten.name, kitten %> // 
         <%= link_to 'Delete Kitten', kitten_path(kitten), data: {
                            turbo_method: :delete,
                            turbo_confirm: "Are you sure?"
                            } %>  
    </li>
  <% end %>
</ul>